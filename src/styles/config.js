// standard values for the styles
const config = {
  primaryColor: '#60C9CD',
  secondaryColor: '#9643C3',
  tertiaryColor: '#FFF',
  primaryTextColor: '#222',
  errorColor: '#F00',
};

export default config;
