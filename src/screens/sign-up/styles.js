import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  titleText: {
    fontSize: '36px',
    fontWeight: 'bold',
    marginTop: '30%',
  },

  loginContainer: {
    marginTop: '5%',
    width: '70%',
    alignItems: 'center',
  },
});

export default styles;
