import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  loginText: {
    fontSize: '36px',
    fontWeight: 'bold',
    marginTop: '60%',
  },

  loginContainer: {
    marginTop: '20%',
    width: '70%',
    alignItems: 'center',
  },
});
