import { StyleSheet } from 'react-native';

// import the style config
// import config from '../../styles/config';

// styles for the recipes screen
const styles = StyleSheet.create({});

export default styles;
